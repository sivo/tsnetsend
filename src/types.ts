export enum Command {
  on = 'on',
  off = 'off',
  dim = 'dim',
  learn = 'learn',
  bell = 'bell',
};

export enum Type {
  switch = 'switch',
};

export enum Model {
  selflearning = 'selflearning',
  codeswitch = 'codeswitch',
  bell = 'bell',
};

